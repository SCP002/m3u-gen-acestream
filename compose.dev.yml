name: m3u-gen-acestream

services:
  dev:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    image: m3u-gen-acestream-dev
    init: true
    command: sleep infinity
    working_dir: /workspace
    volumes:
      - .:/workspace:cached
      - go_mod_cache:/go/pkg/mod
      - go_build_cache:/root/.cache/go-build
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp:unconfined

volumes:
  go_mod_cache:
  go_build_cache:
